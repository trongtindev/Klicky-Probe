# Moonraker update manager for Klicky Probe Plugin ([update_manager klicky_probe])
#
# Single source of truth for the update_manager keys. The installer
# (plugin/install.sh → moonraker_update_block) reads this file and
# substitutes path/origin. Manual install: copy from the first
# [update_manager ...] line into moonraker.conf, adjust path/origin,
# then restart Moonraker.
#
# Keep `origin:` in sync with DEFAULT_ORIGIN in plugin/install.sh
# (installer rewrites origin from `git remote get-url` when present).

[update_manager klicky_probe]
type: git_repo
path: ~/klicky-probe-plugin
origin: https://github.com/trongtindev/klicky-probe-plugin.git
primary_branch: main
install_script: plugin/install.sh
managed_services: klipper
